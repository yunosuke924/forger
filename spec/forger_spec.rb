# frozen_string_literal: true

RSpec.describe Forger do
  it "has a version number" do
    expect(Forger::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
